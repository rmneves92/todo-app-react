import React from 'react'
import ReactDOM from 'react-dom' // Importar apenas aqui. Único arquivo que interage com a DOM
import App from './main/app'

ReactDOM.render(<App />, document.getElementById('app'))